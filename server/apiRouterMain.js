const { generateRoute, generateRouteWithWaypoints } = require('./services/generateRoute');

// Export the main route generation function
module.exports = {
    generateRoute,
    generateRouteWithWaypoints
};
